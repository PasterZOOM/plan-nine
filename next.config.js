/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.gutenberg.org']
    },
    reactStrictMode: false,
    swcMinify: true
};

module.exports = nextConfig;
